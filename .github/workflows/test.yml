name: section-actions
on: workflow_dispatch
jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
            - name: hello world
              run: |
                    echo "Hello World"
                    echo "Bye World"
